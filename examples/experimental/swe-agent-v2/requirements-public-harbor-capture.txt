-r requirements-public-harbor-server.txt
# Native Harbor capture goes through the SDK-owned facade
# (probe.connectors.harbor_capture attach()/finalize(), probe-research >= 0.23.0),
# and sandbox-state snapshots (MILES_SANDBOX_STATE=1) additionally need the
# packaged probe-sandbox-snapshot binaries.
#
# Install from git: the binaries are committed to the repo, so NO Go toolchain
# is needed at install time. The git install sorts above every PyPI release,
# so a resolver will not silently downgrade this to a binary-less wheel.
probe-research @ git+https://github.com/prbe-ai/research-os-agent.git@main
# After 0.23.0 is on PyPI the line above may be replaced with: probe-research>=0.23.0
