# IMPORTANT: environment variables cannot attach storage. In the Instant
# Cluster creation form, select the network volume and mount it at /workspace
# before deploying.

NCCL_SOCKET_IFNAME=ens1
GLOO_SOCKET_IFNAME=ens1
NCCL_DEBUG=WARN

HARBOR_ENVIRONMENT_TYPE=daytona
MILES_HARBOR_ENVIRONMENT_KWARGS_JSON={}
HARBOR_DELETE_ENVIRONMENTS=true
AGENT_MAX_CONCURRENT=2
AGENT_SERVER_TIMEOUT_SEC=14400
MILES_HARBOR_REQUEST_TIMEOUT_SEC=14400

MILES_SCRIPT_EXTERNAL_RAY=1
MILES_SESSION_SERVER_BIND_IP=0.0.0.0
MILES_ROOT=/workspace/miles
HARBOR_DATA_ROOT=/workspace/harbor
MEGATRON_PATH=/root/Megatron-LM

# Inject secrets through Runpod Secrets or interactively on the primary:
# DAYTONA_API_KEY
# HF_TOKEN
# WANDB_API_KEY
# AGENT_SERVER_AUTH_TOKEN
# MILES_SESSION_API_KEY

# Do not set Runpod-generated NODE_*, PRIMARY_*, MASTER_*, WORLD_SIZE,
# RUNPOD_PUBLIC_IP, RUNPOD_TCP_PORT_70000, or RUNPOD_VOLUME_ID values here.
