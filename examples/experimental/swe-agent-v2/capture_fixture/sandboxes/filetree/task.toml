version = "1.0"

# Stress sandbox: the solution adds/modifies/deletes files, writes a binary and
# a nested file, and creates a symlink — so the probe.sandbox-state/1 delta must
# report every change class. Use this to validate capture fidelity on a sandbox.

[task]
name = "miles/capture-fixture-filetree"
authors = []
keywords = ["capture-fixture", "delta"]

[verifier]
timeout_sec = 30.0

[agent]
timeout_sec = 30.0

[environment]
build_timeout_sec = 120.0
cpus = 1
memory_mb = 512
storage_mb = 1024
gpus = 0

[verifier.env]

[solution.env]
